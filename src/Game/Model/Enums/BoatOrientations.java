package Game.Model.Enums;

/**
 * Contains possible boat orientations when designing
 */
public enum BoatOrientations {
    /**
     * Ship is horizontal (left to right)
     */
    HORIZONTAL,
    /**
     * Ship is vertical (top to bottom)
     */
    VERTICAL
}
